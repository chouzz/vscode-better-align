// Only some comments
  // Only some comments
    // Only some comments
      // Only some comments
        // Only some comments

var abc = 123;
var fsdafsf = 32423,
fasdf = 1231321;

let fdsafa:fadsf = fdsaf;
let lt:LineInfo = {
  line:textline
  , sgfntTokenType:TokenType.Invalid
  , tokens:[]
};

// AutoHotKey Syntax
test := 1
teastas := 2