// Only some comments
  // Only some comments
    // Only some comments
      // Only some comments
        // Only some comments

var abc = 123;
var fsdafsf = 32423,
fasdf = 1231321;

let fdsafa:fadsf = fdsaf;
let lt:LineInfo = {
  line:textline
  , sgfntTokenType:TokenType.Invalid
  , tokens:[]
};

// AutoHotKey Syntax
test := 1
teastas := 2

// Php Syntax
$data = [
  self::LAUNCHAAA  => 'test',
  self::WAIT   => 'testtas',
];

var abc == 123;
var fsdafsf == 32423,
fasdf != 1231321;

var abc === 123;
var fsdafsf === 32423,
fasdf !== 1231321;

public function index()
{
    $x = 'test';
    if (
        $x === 'nott test'
        || $x === 0
        || $x !== 'test'
    ) {
        return false;
    } else {
        return true;
    }
}